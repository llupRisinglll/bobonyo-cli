# Report tracker

Every user report and its status, so nothing gets lost between turns. A report
is DONE only when verified live (or unit-tested) and committed.

| # | Report | Status | Verified / committed |
|---|---|---|---|
| 1 | Rename status `⏵⏵⏵ yolo mode on` → `⏵⏵⏵ yolo` | ✅ DONE | status line + `/status` modal show `yolo`; live capture |
| 2 | Test web-search fallback (search model = deepseek-v4-flash, main = mimo-v2.5-pro) | ✅ DONE | live: `✦ WebSearch fallback: deepseek-v4-flash searched → mimo-v2.5-pro responds` + real results |
| 3 | Test image fallback (vision model = mimo-v2.5, main = mimo-v2.5-pro) | ✅ DONE | live: `✦ Vision fallback: mimo-v2.5 analyzed 1 image → mimo-v2.5-pro responds`; the main model correctly described the image from the vision description |
| 4 | Add connecting to Codex as a provider feature | ✅ DONE | `/codex` command scaffolds the Codex provider (api.openai.com/v1, API key prompt, live model discovery, context window); appears in suggestions |
| 5 | Add a right-side gap for the scrollbar | ✅ DONE | scrollbox `paddingRight={2}`; `historyFillWidth` adjusted; content no longer touches the scrollbar |
| 6 | Responses must never render at column 0 (wrap in a container) | ✅ DONE | replies render in their OWN padded markdown container (`paddingLeft={2}`); verified: reply paragraphs, headings, lists, code all sit at column 2+; non-reply content (user msgs, tool rows) is unaffected |
| 7 | System prompt: say BoboNyo (not nanocoder); be blunt/snobbish, honesty first | ✅ DONE | both system prompts (full + nano) updated |
| 8 | Bug: typing `/rename` + space, cursor does not move (space invisible) | ✅ DONE | root cause: `wrapTextDetailed` trimmed trailing spaces so the caret snapped before the space; trailing spaces are now preserved + the caret maps after them; live capture `❯ /rename ▌` |
| 9 | Bug: `/rename    asdasd` multiple spaces looked like a tab | ✅ DONE | same root cause as #8; spaces now render normally |
| 10 | Keep a report tracker md with status | ✅ DONE | this file |
| 11 | [PRIORITY] Chatbox flashes during "Working…" (christmas lights) | ✅ DONE | root cause: the transcript memo rebuilt EVERY markdown block each 100ms tick (spinnerFrame dependency). Split into SETTLED blocks (no ticker deps, stable identity) + a LIVE block (streams alone); verified: settled rows byte-identical across 3 captures 1s apart during streaming |
| 12 | Pressing a key should pause the cursor blink (visible while typing, debounce-resume) | ✅ DONE | `cursorForced` signal + 900ms debounce timer on every keypress |
| 13 | Fast backspace/erase (hold to accelerate, opencode parity) | ✅ DONE | `handleBackspace` deletes up to 4 chars per repeat once held |
| 14 | Sent user messages should color commands + REAL [Box #n] tokens | ✅ DONE | `ChatMessage.attachments` records real attachment paths; the usermsg fence carries the real token numbers; `tokenizeUserMessage` colors known `/commands` + real tokens only (manual `[Image #1]` stays plain). Unit-tested |
| 15 | Tool details modal missing color; arrow keys leaked to the chat | ✅ DONE | modal lines tokenized (primary tool names, secondary output); history + app key handlers gate on `detailsOpen` so arrows only scroll the modal |
| 16 | All expand opens a modal (incl. thought); collapsed previews cap at 3 lines | ✅ DONE | thoughts, single tool rows and compact tallies all stash expanded details; clicks open the DetailsModal; `PREVIEW_LINES` = 3 |
| 17 | Reply `✦` must align with tool glyphs (column 1), NOT indented | ✅ DONE | the reply glyph renders OUTSIDE the markdown container (row: glyph + content box); content + re-wraps start at column 3 |
| 18 | Replace the caret with a box background (opencode-style) | ✅ DONE | the char under the cursor is highlighted with the active-row bg/fg; text never moves; verified insertions land correctly |
| 19 | [CRITICAL] Chat history still blinking (regression) | ✅ DONE | deeper root cause: OpenTUI's `<For>` re-renders EVERY child when the `each` array reference changes, and the combined render-blocks array was rebuilt each tick. The LIVE block now renders in its OWN `<Show>` node OUTSIDE the settled `<For>` (whose `each` reference is stable), so the ticker can never touch settled blocks. Verified: settled rows byte-identical across 5 captures 1s apart during streaming |
| 20 | Invert `/` suggestions: selected at the BOTTOM, navigation goes UP | ✅ DONE | the suggestion window renders REVERSED (first match at the bottom row) and ↑ walks FORWARD (content scrolls up), ↓ back. Verified live |
| 21 | Cursor blink still moved the text (added a space) | ✅ DONE | the caret cell now ALWAYS renders (the char under the cursor, or the LAST char at end-of-line, or a space on an empty line), highlighted when visible and PLAIN when hidden, so the line width is constant. Verified: input row length identical across 6 blink frames |
| 22 | Response indentation/formatting inconsistent while rendering vs done; format in realtime | ✅ DONE | the LIVE region now renders the STREAMING REPLY in the SAME glyph-row container as settled replies (glyph at column 1, content at column 3), so markdown formats in realtime and the settled shape is identical; the live THOUGHT stays in its own unpadded node. Verified mid-stream |
| 23 | Re-test image + web-search fallback models | ✅ DONE | live on mimo-v2.5-pro: web search (deepseek-v4-flash) returned results + indicator; image (mimo-v2.5) analyzed the test image + indicator, and the main model correctly described it ("solid, uniformly filled medium blue vertical rectangle") |
| 24 | Color the vision-fallback message YELLOW | ✅ DONE | new `kind: 'warning'` message + `warningrow` renderer; the indicator renders in the theme WARNING color (verified `38;2;224;175;104` live) |
| 25 | Cursor renders AFTER the character (not on the last one) | ✅ DONE | `caretIndexFor` now places the caret cell AFTER the last char (a highlighted trailing space) at end-of-line; the cell always renders so width stays constant (no movement) |
| 26 | `/connect` should go through the connect-provider flow (Codex included) | ✅ DONE | `/connect` now asks "Connect provider: codex (OpenAI) or custom?"; `codex` routes to the Codex scaffold (API-key prompt), `custom` to the full wizard; `/connect codex` shortcuts |
| 27 | Fix /model + /resume title spacing (opencode-like) | ✅ DONE | both cards got `paddingY=2` + a comfortable two-row gap between the title and the search field; card heights bumped so the list never overflows |
| 28 | [CRITICAL] Flicker returns on thinking AND any tool call | ✅ DONE | root causes: (a) the settled memo read `spinnerFrame` via the tool/agent row blink, re-running the whole settled memo every 100ms; (b) running tool rows lived in the settled memo and re-read `liveOutputs` per output tick; (c) live nodes re-parsed markdown per word/100ms. Fixes: settled rows never blink (`blinkOn=true`), running tool rows moved to the live region, and a NEW `LiveStreamRows` util renders ALL live content (thinking + running tools) as PLAIN TEXT so OpenTUI only repaints changed cells; reasoning/reply tails are throttled (~150ms). Verified: settled rows stable during tool streams (only completion transitions), thinking block updates ~1-2/s |
| 29 | Detect LSP + MCP configured in nanocoder | ✅ DONE | MCP now reads the ORIGINAL's `.mcp.json` (`{mcpServers: {...}}`, `{id, command, args, env}`) from the nanocoder config dir (the old code read a non-existent `mcp.json` from the wrong path); `/status` shows configured + connected servers. LSP auto-discovery added (`src/lsp.ts`, parity with nanocoder's server-discovery) detecting installed language servers (typescript-language-server, rust-analyzer, clangd, …). Verified live |
| 30 | Breakline before every response | ✅ DONE | reply blocks render a leading blank row (opencode-style reply margin). Verified: `❯ md test` then blank then `✦ Heading` |
| 31 | `--continue` / resume argument on startup | ✅ DONE | `--continue` is an alias for `--resume last` (or `--continue <id>`); verified `bun run dev --continue` resumed the last session |
| 32 | Keep the welcome banner on an EMPTY chat; lazy-load indicator animated above the input; no MCP logging | ✅ DONE | MCP connect/fail messages were appended to the chat (making it non-empty and hiding the banner); removed. The animated `⠸ Loading skills · tools · MCP · LSP…` indicator sits ABOVE the input box and clears when init finishes (LSP discovery pre-warmed in the same pass so `/status` opens instantly). Verified live: banner shows on empty, indicator animates above the input, MCP/LSP only in `/status` |
| 33 | `/status` shows details already visible on the status line + input corner; remove the duplicates | ✅ DONE | removed Model (input corner shows `model[effort]`), Context (`ctx ~N%`), Mode/Tune/Agents/Background (status line) and Directory (cwd on the status line) from the `/status` modal. It now lists only what is NOT visible elsewhere: Session, Provider, Messages, Checkpoints, Skills, Custom commands, MCP servers, LSP, Steering, Watchdog, Stream guard, Version. Regression test asserts the removed rows never return. Unit-tested + typecheck + build |
| 34 | Running tool rows render ENTIRELY primary (glyph included); glyph must blink secondary + text match the done state | ✅ DONE | `LiveStreamRows` only supported one header color, so `✦ Name(detail)` came out all primary-bold. It now renders a segmented header: the glyph is SECONDARY and BLINKS on the 500ms ToolGlyph cadence (width-stable, hidden frame keeps a space) via a new pure `glyphBlinkOn` helper; the tool name stays primary bold and the detail secondary, exactly like settled rows; output lines remain secondary/dim under `  └   `. Unit-tested (blink cadence) + typecheck + build |
| 35 | The MCP/LSP loading indicator is not animated; one service can finish before the other; animate glyph + dots | ✅ DONE | `startupLoading` is now a list of per-service rows (`Loading skills · tools`, `Loading MCP`, `Loading LSP`), each clearing INDEPENDENTLY when its scan finishes (file reads vs stdio handshake vs sync binary scan). Each row renders a BLINKING secondary `✦` glyph (500ms ToolGlyph cadence) + ANIMATED dots (1→2→3 every 200ms), above the input box. A 600ms minimum-visible floor guarantees the animation is perceivable even when everything finishes in one tick. New `loadingDots` helper unit-tested |
| 36 | Renaming an EMPTY conversation hides the welcome banner | ✅ DONE | the banner condition used `messages().length === 0`, so the `Session renamed to …` log made the transcript non-empty and killed the banner. New pure `hasConversation()` treats only rows WITHOUT a `kind` as real conversation (user/assistant/tool); info/warning system logs never hide the banner. Unit-tested |
| 37 | Session logs like `Session renamed to "x".` should be YELLOW with the `✦` glyph | ✅ DONE | `/rename` now uses `appendWarning('  ✦ Session renamed to "…".')`, rendering through the existing `warningrow` surface (theme WARNING yellow + diamond glyph, parity with the vision-fallback indicator) instead of a plain dim info row |
| 38 | [PRIORITY] Running tool rows regressed: colors/spacing differ from the done state (bash loses syntax highlighting, glyph all-primary earlier); hover still flickers EVERYTHING | ✅ DONE | LIVE tool rows now go through the SAME `formatToolEntry` + fenced-row + tokenizer pipeline as SETTLED rows (`status: 'running'`): bash commands get real syntax highlighting, names stay primary bold, details secondary, `  └   ` output dim with the same wrap/cap/+N footer — the only differences while running are the BLINKING secondary glyph and the streaming tail; on completion the row settles byte-identical with a static green glyph (verified live in preview: `✦ Bash(for i in …)` with keyword/string/number colors, tail streaming `line 3…16` + `+N lines`, then green static glyph on done). Output re-parses are throttled (~7/s, never per chunk) and the markdown content setter no-ops on unchanged values, so idle ticks never repaint. HOVER flicker root cause: the settled memo read `hoveredBlock()` and rebuilt the whole blocks array on every hover (OpenTUI For re-renders ALL children on array identity change). The memo now returns STABLE parts with NO hover read; the `:hover` marker is applied per-item in the For render, so only the hovered markdown node re-parses (verified: hover repaints only the hovered bash block, header untinted, body tinted). Removed the now-unused plain-text `LiveStreamRows` |
| 39 | Flicker came BACK while a tool renders; hover highlights only text (not whole rows) AND the header; clicking highlights other text; modal colors only the first char after the glyph | ✅ DONE | (1) LIVE tool rows no longer go through `<markdown>` at all: each row is tokenized ONCE per throttled update with the SAME settled tokenizers and rendered as PLAIN text cells (new pure `live-tool-row.ts`), so OpenTUI repaints only changed cells — same colors/spacing as settled, ZERO re-parse flicker, glyph blinks in its own cell. (2) Hover is now a pure OVERLAY: a full-width semi-transparent Box positioned over the hovered block's BODY rows only (header excluded by construction). The old text-buffer bg tint was removed — OpenTUI's buffer filled EVERY line (header included) whenever any chunk had a bg, which no chunk-level fix could stop. Content never changes on hover now, so nothing re-parses or repaints except the overlay. (3) Clicking a tool clears the hover state before opening the details modal, so no highlight lingers behind it. (4) The modal's line colorizer was extracted (`colorDetailLine`, unit-tested): the tool NAME is the whole word up to `(` (primary bold), glyph/detail secondary — the old non-greedy regex colored only `B` in `Bash(...)`. Verified live in herdr: overlay covers only body rows (header untouched), modal shows `Bash` primary bold + detail secondary, settled glyph green, no lingering highlight behind the modal |
| 40 | Running tool rows have NO breakline (they glue to the user message and to each other); add automated + detection tests so these regressions can never return | ✅ DONE | (1) New foolproof `LiveToolRows` component — the ONLY renderer for running tool rows: plain text cells (never markdown), the blinking glyph in its own cell, and a LEADING BREAKLINE per row so the live layout matches the settled blank rows exactly. The live thought gets the same leading blank. Verified live in herdr: `/mock:bash` shows `❯ run bash` → blank → `Bash(...)`, `/mock:tools` shows a blank between every live row, and the settled state is byte-identical in spacing. (2) `regression-guards.spec.ts` — source-scan DETECTION tests that fail the suite if a future change (even by a dev who doesn't know the history) reintroduces: markdown in the live rows, a live row not going through `liveRowSegments`, a missing per-row breakline, or the old `applyHoverBackground` buffer-bg hover instead of the overlay. (3) Behavioral parity test: live body lines are byte-identical to the settled `formatOutputTail` output (spacing can't drift). 113 tests pass |
| 41 | Hover on tools doesn't stick (blinks off) and highlights the wrong rows after mock:tools — the chat-history pipeline is hostile to mouse events | ✅ DONE | ARCHITECTURE FIX, as the user diagnosed: settled tool/thought rows no longer render through markdown/CodeRenderable at all. They now render as PLAIN COMPONENTS (`SettledToolRow`), the same pattern the settings rows use: the hover highlight is a per-row `backgroundColor` INSIDE the body rows (header excluded by construction, full-width), so there is NO overlay geometry to compute and NO hit target that changes under the cursor (the overlay appearing under the pointer fired an `out` event that bubbled to the scrollbox and cleared the hover — the "doesn't stick" bug). The old absolute-overlay row arithmetic (which drifted when the transcript scrolled) is deleted. `handleMouseOut` also keeps the hover when the event still maps to a transcript row. Verified live in herdr: hover tints exactly the hovered block's body rows (header clean) with and without scroll, thoughts render `⚙ Thought` via the same component, spacing/breaklines preserved. Regression guards updated: settled tool rows must render via `SettledToolRow`, `hoverOverlay`/`applyHoverBackground` must never return. 113 tests pass |
| 42 | `bobonyo` run in Hilinga loads no skills and `/status` doesn't say which AGENTS.md was loaded; the "Worked for…" line disappears after a few seconds | ✅ DONE | (1) LAUNCHER WAS WRONG: `dist/bobonyo` and the alias `cd`'d into the repo before running, so `bobonyo` in /project ran with cwd=/repo — skills/AGENTS.md resolved against the wrong directory (the Hilinga report). Both launchers now resolve the repo WITHOUT cd'ing (user cwd preserved). (2) That fix exposed a startup crash in ANY untrusted/foreign cwd: without the repo's bunfig, bun skipped the OpenTUI preload and the UI died with "Orphan text" (the preload registers the JSX transform before the module graph is processed). The launcher now passes the preload by ABSOLUTE PATH (`-r node_modules/@opentui/solid/scripts/preload.js`) so it works even when the user's project has its own node_modules or no bunfig. (3) Trust/approval prompts dereferenced null signals (`prompt()!.question`, `approval()!.name`) during the first-render race in untrusted dirs — guarded with optional chaining. (4) `/status` now reports the resolved rules file: new pure `resolveRulesFile()` walks UP from the cwd for the nearest AGENTS.md (cwd wins) and both the system prompt and the status modal use it — verified live in Hilinga: `/status` shows `AGENTS.md /mnt/data/KSProjects/Hilinga/AGENTS.md`, Skills correctly `0 loaded` (empty folder). (5) "Worked for…" no longer auto-expires: the 8s timeout was removed; it persists during idle and clears only on the next turn or `/clear`. Unit tests: `resolveRulesFile` (cwd/parent/none), status row, launcher + null-signal regression guards. 122 tests pass |
| 43 | `/status` LSP row is never green even though servers are detected | ✅ DONE | LSP detection WAS working (typescript-language-server, rust-analyzer, clangd) — the row's color logic looked for the literal "no issues" in the label, which the label (the server LIST) never contains, so it stayed uncolored. The row is now GREEN when servers are detected, warning when none are detected or when real diagnostics issues (`N issues`) are present. Verified live in Hilinga: `LSP` value renders `38;2;122;247;120` (success green). Unit-tested: detected → success, none → warning, issues → warning. 124 tests pass |
| 44 | Separate aliases: `bobonyo` = dist RELEASE only, `bobonyo-dev` = live SOURCE; code changes must never close a running release instance | ✅ DONE | `~/.local/bin/bobonyo` now runs ONLY the built `dist/bobonyo` launcher (the `--dev` flag branch was removed — dev is a separate command). New `~/.local/bin/bobonyo-dev` runs the live source (`bun run src/index.tsx`) with the absolute OpenTUI preload and preserves the user's cwd, same as the release launcher. `bobonyo-preview` was also fixed (no more cd into the repo; keeps the user's cwd + preload). A running bun process loads its modules once at startup, so editing source or rebuilding dist never touches a running instance — and the two aliases are fully independent processes. Verified: `bobonyo` and `bobonyo-dev` both run from Hilinga simultaneously. Regression guard: the dist launcher must not contain a `--dev` branch. 124 tests pass |
| 45 | First-run trust prompt is confusing — it looks like the chat input is ready (`Trust this directory? (y/n): ▌`) and the question color (primary) doesn't fit | ✅ DONE | The trust gate now renders a CO-DEX-STYLE DIALOG (`TrustModal`): a centered card over a dimmed backdrop with a WARNING-colored `⚠ Trust this directory?` title, an explanation ("bobonyo can read and write files and run commands here"), the directory path, and two explicit selectable rows — `❯ Yes, trust this directory` (default, like codex's [Y/n]) and `No, do not trust`. Keyboard: ↑/↓ select, Enter/y confirm, n/Esc decline; mouse hover/click works on both rows; every key is owned by the dialog so nothing leaks to the chat. Esc/No still EXITS (never runs untrusted). The free-text prompt row stays only for real wizard inputs (settings values, API keys, connect flows). The old `trustDecision` y/n parser was removed. Verified live from /tmp: dialog renders (title yellow 224;175;104, active row info-bg + ❯), `n` declines and exits, Enter trusts and continues into the chat, and the directory is added to trustedDirs. Regression guard: the trust gate must use `TrustModal`, and the input box must never render "Trust this directory". 123 tests pass |
| 46 | `/resume` row layout: currently `last_prompt` above `conversation_name`; want `session_id: conversation_name` first, then ` └ last_prompt` (secondary) | ✅ DONE | Session rows now render the TITLE first — `session_id: conversation_name` (pure `sessionLabel()` helper: the name is OMITTED when it's missing or still the default "New conversation", so untitled sessions show just the id), with the "how long ago" label still on the right. The LAST PROMPT sits BELOW it with a ` └ ` branch in the secondary color (dim), the same optional-line color rule as everywhere else. Unit-tested: named → `id: name`, default/missing name → `id`. Verified live in /resume: `❯ sess_msp17z5r_1  15m ago` over ` └ Create a Hilinga worktree…`. 126 tests pass |
| 47 | `/resume` should show only CURRENT-FOLDER conversations by default, with a hotkey to toggle ALL | ✅ DONE | Sessions now record the folder they were created in (`cwd` persisted in the session file and surfaced by `listSessions`; new sessions stamp `process.cwd()`). The `/resume` modal filters to the current folder by default (pure `sessionInFolder()`: legacy sessions without a cwd are always shown so nothing silently disappears), and **Ctrl+A toggles the scope** — a scope indicator under the search shows `Showing conversations in <cwd> · Ctrl+A: all` vs `Showing all conversations · Ctrl+A: this folder`, and the footer hint reflects the active scope. A plain "a" still types into the search box. Unit-tested: session cwd round-trip (temp data dir) + `sessionInFolder` (current folder / all / legacy). Verified live: indicator shows the cwd scope, Ctrl+A flips it. 130 tests pass |
| 48 | Adhoc: create a demo GIF of the harness CLI, starting from running `bobonyo` | ✅ DONE | Recorded with `asciinema rec` (herdr/tmux have no record feature; asciinema + agg is the standard tmux-era pipeline) at 100x30, idle-capped at 1.5s, rendered with `agg --speed 2 --fps-cap 24`. The cast was trimmed after the app's exit and the goodbye banner re-printed on a cleared screen so the closing frames are clean (agg keeps the last alt-screen frame visible through the alt-screen exit otherwise). The GIF shows: shell prompt → `bobonyo` → banner + lazy-load rows → typed prompt → real `mimo-v2.5-pro` run with streamed thinking (animated gear + live timer), `✦ LS(...)` tool rows with streaming output, `✦ Ran LS ×3` compaction, final markdown answer, `✦ Worked for a thoughtful 11s. · 13897 tokens`, then `/exit` and the goodbye banner. Committed as `demo/demo.gif` (402 KB, 979x694, ~53s); moved to `docs/demo.gif` in row 52 |
| 49 | `/model` modal should open with the CURRENT model highlighted by the navigation indicator and start navigation there | ✅ DONE | `initialModelRowIndex()` (pure, unit-tested) picks the row of the current model, falling back to the first model / Inherit row, and the modal now initializes `rowIndex` from it instead of hardcoding 0 (which landed on the provider header and showed NO highlight). The list also SCROLLS the current row into view (`scrollStart = max(0, index - listVisible + 1)`), so it is highlighted even on short terminals — verified live at 80x24 where `mimo-v2.5-pro (current)` is the 3rd model row and was previously below the fold. After a query change the selection re-snaps through the same helper (typing `asr` highlighted `mimo-v2.5-asr`). Regression guards: the modal must init `rowIndex` via `initialModelRowIndex(buildRows())` (never `createSignal(0)`) and re-snap via `createEffect`. 142 tests pass |
| 50 | `/resume` search should also match the SESSION ID | ✅ DONE | The filter now goes through the pure `sessionMatchesQuery()` helper, which matches the session id (`sess_…`), the conversation name, AND the last prompt — case-insensitive, empty query matches all. Unit-tested (id exact/partial/case, name, prompt, empty, missing fields). Verified live: typing `msp2ho14` into the resume search filtered the list down to exactly `sess_msp2ho14_1`. Regression guard: the resume filter must call `sessionMatchesQuery(session, q)`. 142 tests pass |
| 51 | Ctrl+←/→ should jump BETWEEN WORDS in the input (original repository parity) | ✅ DONE | Mirrored the ORIGINAL nanocoder `text-input.tsx`: whitespace-delimited word jumps (spaces AND newlines, readline Alt+B/F), with the target snapped OUT of atomic `[Image #N]` / `[Text #N]` tokens so the caret never splits a block (`snapOutOfAtomicToken`, parity with the original's `snapOutOfPlaceholder`). OpenTUI reports Ctrl+arrow as `{name:'left'|'right', ctrl:true}` (verified in the parser), so the arrow handler branches on `event.ctrl`. Pure helpers `moveToPrevWord` / `moveToNextWord` / `snapOutOfAtomicToken` are unit-tested with the ORIGINAL spec expectations (middle-of-word, runs of spaces, newline crossing, start/end stays). Regression guard: the arrow handler must branch through the word-jump helpers. 152 tests pass |
| 52 | Put the demo GIF in `docs/` and REPLACE the ASCII banner in the README with it | ✅ DONE | `demo/demo.gif` moved to `docs/demo.gif` (empty `demo/` dir removed). The `## What it looks like` ASCII-art banner block (and the now-redundant separate `## Demo` section) was replaced with the GIF image (`![bobonyo in action](docs/demo.gif)`), keeping the prose paragraph below it. 152 tests pass |
| 53 | Thought blocks: the settled gear turned GREEN, and long reasoning lines wrapped OUTSIDE the `└` indentation | ✅ DONE | (1) `SettledToolRow` colored every glyph with `glyphColor(status)` — done = success GREEN — so a settled `⚙ Thought` gear was green like a finished tool. New pure `settledGlyphColor(glyph, status, palette)`: THOUGHTS are always secondary/dim (optional info, static gear), only tool `✦` follows the status. (2) The thought body used a bare `└ ` + 3-space continuations (tools use `  └   ` + 6-space) and long lines wrapped at OpenTUI's left edge, escaping the indent. New pure `wrapThoughtBody(text, width)` pre-wraps reasoning to the chat width with the TOOL container format (`  └   ` lead, `      ` continuations — parity: `formatOutputTail`), used by BOTH the settled fence and the live thinking tail, so wrapped lines and explicit newlines always stay inside the `└`. The collapsed footer also switched to the tool 5-space `… +N more lines`. Verified live in herdr: a long `⚙ Thought (32s)` body wraps with the continuation aligned under the content. Regression guards: `SettledToolRow` must use `settledGlyphColor` (never bare `glyphColor(props.status)`), thought bodies must go through `wrapThoughtBody` (`tailLines` removed). 160 tests pass |
| 54 | The live "Thinking" header lost its animation: static gear and dots AFTER the timer | ✅ DONE | The header is now built by the pure `liveThinkingHeader(frame, elapsed)`: ANIMATED gear `⚙ ↔ ✦` (same secondary color — glyph animation, never a color blink) + spinner dots (`workingDots`, 1→2→3) placed BEFORE the real-time timer: `⚙ Thinking ... (12s)`. It reuses the app ticker's `spinnerFrame` like the Working indicator. Unit-tested: gear alternates across frames, dots animate 1→2→3 and precede the timer, timer keeps the `1m 2s` format. Regression guard: the live header must call `liveThinkingHeader(spinnerFrame(), …)` (the old static `Thinking · (Ns)...` fails the scan; the elapsed arg is now `thinkingElapsed()` per row 55). Verified live in herdr: rapid sampling caught both `⚙ Thinking . (Ns)` and `✦ Thinking .. (Ns)` variants. 163 tests pass |
| 55 | The thinking timer over-counts: it measured the WHOLE turn (from message send), so a few seconds of thinking showed `(7s)`; the settled `⚙ Thought (Ns)` did the same | ✅ DONE | New `thinkingElapsed` signal + `thinkingStartedAt` anchor: the timer starts when reasoning FIRST streams (the `onReasoning` hook anchors it on the first delta of each round, `reasoning()` is reset per round), NOT at message send — so pre-thinking provider latency and queue time never count. The 1s app ticker now also updates `thinkingElapsed` from the anchor; the live header calls `liveThinkingHeader(spinnerFrame(), thinkingElapsed())`. The settled `⚙ Thought (Ns)` uses the same anchor via pure `thinkingSeconds(startedAt, now)` (rounded, min 1s) instead of `Date.now() - startedAt`. Reset on submit, per-round end, interrupt and `/clear`. Unit-tested (`thinkingSeconds` rounding/clamp) + regression guards (anchor in `onReasoning`, `thinkingSeconds` helper, header uses `thinkingElapsed`). Verified live in herdr: the header appears at `(0s)` and ticks up from there. 165 tests pass |
| 56 | Assistant replies render `✦The` — the glyph glues to the first word (no visible gap) | ✅ DONE | The reply row rendered `✦{' '}` as one text element; OpenTUI trims the trailing space, so the markdown box started on the next cell and the first word sat directly after the glyph. The glyph is now its OWN cell (`✦`) and the content box carries `paddingLeft={2}`, so replies render `✦  Hello world.` with a guaranteed gap and wrapped lines stay inside the container (same for the live streaming reply). Regression guard: the reply content boxes must carry `paddingLeft={2}` and the old glued `✦{' '}` must not reappear. Verified live in herdr: `✦  Hello world.`. 166 tests pass |
| 57 | Record the demo INSIDE a herdr pane (the user's preferred environment), with smooth typing and NO emoji-looking glyphs in the GIF | ✅ DONE | Re-recorded via `asciinema rec` run inside a fresh herdr pane (`herdr pane run/send-text/send-keys` drove the app; pane created from `w1:p1A`, closed after). Typing was sent ONE CHARACTER at a time (~0.12s apart), so the prompt types smoothly instead of in 5-char bursts. Rendered with `agg --speed 2 --fps-cap 24 --emoji-font-family "Adwaita Mono"` — Adwaita Mono covers every UI glyph (⚙ ★ ⏵ ◕ ✦ ⌕ ➜ ❯) as MONOCHROME, so the gear/star are text glyphs, never color emoji (verified: 0 colorful pixels in the gear cell on a test render; the old default Noto Color Emoji was the culprit). The recording captures all recent fixes: animated `⚙↔✦ Thinking … (0s)` header with dots before the timer, secondary thought gears, `  └   `-wrapped thought bodies, `✦  ` reply gap, and the clean exit banner. `docs/demo.gif` replaced (367 KB, 979x694, ~27.6s). 166 tests pass |
| 58 | The herdr demo GIF looks washed out and the `· ctx ~0%` corner on the bottom right looks broken | ✅ DONE | Root cause: `asciinema rec` inside herdr records the SOURCE terminal's theme in the cast header (`term.theme.bg = #232627` from libghostty). agg fills every default-background cell with that color, so the app's dark UI sat on a gray backdrop — colors lost contrast and the dim secondary corner text (`─ · ctx ~0% ╯`) became hard to read / looked broken. The app only emits `[49m` (default bg) for empty areas, so this is purely a RENDER-time artifact, not an app bug. Fix: the new `scripts/render-demo-gif.sh` strips `term.theme`/`term.version` from the cast header before rendering (plus the existing exit-trim + Adwaita Mono flags), restoring the intended dark background. Verified: the theme-stripped render's background and the corner region are PIXEL-IDENTICAL to the earlier accepted GIF. `docs/demo.gif` replaced (369 KB, 979x694, ~27.6s). 166 tests pass |
| 59 | The `mimo-v2.5-pro · ctx ~0%` corner STILL looks broken in the GIF (separator marks between the elements) | ✅ DONE | The corner label uses NON-BREAKING SPACES (`\u00A0`) around `· ctx ~0%` — opaque in OpenTUI so they cover the border dashes (verified live in herdr: clean corner). agg renders U+00A0 as a VISIBLE GLYPH, so the GIF showed little marks between the corner elements (isolated A/B/C-over-dash tests: `\xa0` = 302 marks, plain space = 174 marks, no-dash control = 302 — proving the marks are the `\xa0` glyphs, not dash bleed). The app stays as-is (live is correct); `scripts/render-demo-gif.sh` now rewrites `\u00A0` → space in the cast output events before rendering (agg paints plain spaces as opaque blanks). Verified: the fixed corner row shows clean gaps where the marks used to be. Recording WAS the latest app (corner code unchanged since before it). `docs/demo.gif` replaced (369 KB, 979x694, ~27.6s). 166 tests pass |
| 60 | The compact tally `Ran Read ×2` renders ALL primary — `Ran` and the `×N` should be WHITE, only the tool name primary | ✅ DONE | Root cause: `liveRowSegments` strips the leading `✦`/`⚙` before tokenizing (the glyph renders/blinks separately), but `groupHeaderChunks` (and `tokenizeAgentRow`) REQUIRED the glyph in their regexes, so the glyph-less `Ran Read ×2` fell through to the generic header path that colors the whole line primary. The `Ran … ×N`/`Ran agent:…` regexes now make the glyph OPTIONAL and still emit: glyph (status color), `Ran ` (white), tool name (primary bold), ` ×N`/separators (white), hint (secondary). Unit tests added for the GLYPH-LESS form (the real pipeline — the old tests only covered the with-glyph form and missed the bug); regression guard scans for the optional-glyph regexes. Verified live: the app now emits `Ran ` white (255,255,255), `Read` primary bold (187,154,247), ` ×2` white, glyph green when done — in both the running and settled tallies. 170 tests pass |
| 61 | The latest demo recording has too much idle time | ✅ DONE | The v3 cast was 213s (50.7s pre-launch pause + an 18s welcome/Lazy-Load stretch + a 118s static finished screen before `/exit`) but rendered to a 57.6s GIF because every frame repaints with different SGR/cursor state, so agg's idle-time limit cannot collapse it. New `scripts/compress-demo-cast.py` replays the cast into a minimal virtual screen and drops redundant repaints of char-identical stretches (keeping ≤0.8s of each), caps gaps >1.2s (the pre-launch 50.7s pause, mid-typing pauses), and preserves the animated working/streaming cadence — wired into `scripts/render-demo-gif.sh` before the theme/`\u00A0`/exit trimming. Verified: the same v3 cast now compresses 213.3s → 30.0s and renders to a **17.95s GIF** (359KB) with the full flow intact: launch banner + animated Lazy-Load dots, smooth typing, streamed thoughts/tools/reply, and the exit banner. The working phase was replayed frame-by-frame: no spinner-only stretch >1.5s survives (every kept frame is real content growth or typing), so the demo stays tight without losing the animation. 170 tests pass |
| 62 | DeepSeek integration: live balance on the status line (`Cred: $n`), auto-fetched models, prompt-cache hit/miss awareness + cost alert, multi-instance-safe caching | ✅ DONE | New `src/deepseek.ts`: `GET /models` (auto catalog, `deepseek-v4-flash`/`deepseek-v4-pro` fetched live) and `GET /user/balance` (USD preferred, `Cred: $5.18` on the status line, label secondary + amount primary) driven by the DeepSeek API docs. Both are cached in `~/.config/nanocoder/deepseek-cache.json` with TTLs (models 1h, balance 5m), atomic temp-file+rename writes (concurrent instances never corrupt or flood: fresh cache short-circuits the fetch, a failed balance fetch falls back to the stale value, and an in-process in-flight map dedupes parallel `/model`/`/status` opens). Refreshed on app start, `/status` + `/model` opens, and a 5-minute interval. Prompt-cache: `prompt_cache_hit_tokens`/`prompt_cache_miss_tokens` are parsed into the completion line (`· cache hit 0%`), a new `/status` "Prompt cache" row (green ≥30% hit, warning below), `/usage` lines, and a toast alert when a sizeable turn (>2k cached tokens) misses >70%. 21 unit tests: detection, `Cred` formatting, fetch parsing, TTL/fresh-vs-stale/atomic/multi-instance/in-flight-dedupe, cache stats + alert thresholds + normalized-snapshot keys. Verified LIVE against the real DeepSeek API (mimo → `--provider DeepSeek`): status line showed `· Cred: $5.18`, `/model` listed both fetched models, a real turn streamed `✦ Thinking … (0s)` then `✦ OK`, fired the toast `Cache miss 100% on this turn · costs more` on the cold start, and the completion line read `✦ Worked for a swift 1s. · 13414 tokens · cache hit 0%`. One startup crash found + fixed during live verification: this OpenTUI Solid fork evaluates memos eagerly, so referencing `credSegment` before its `const` declaration in the status component threw a TDZ ReferenceError that exited the app with code 1 — moved the helper above the memo that uses it. 192 tests pass |
| 63 | The DeepSeek demo GIF is broken: it REPLACED the "What it looks like" demo and the texts are not properly rendered | ✅ DONE | Root cause 1 (wrong file): the DeepSeek recording was written over `docs/demo.gif` instead of its own file. Root cause 2 (broken text): `scripts/compress-demo-cast.py` dropped the status line from recordings. OpenTUI paints incrementally, so the status line (e.g. `· Cred: $5.06`, or `⏵⏵⏵ yolo · tune: full`) is emitted ONCE when it first changes and never re-painted while it stays static. The compressor's "drop redundant repaints" test compared only event TIMESTAMPS: a frame whose content changed at the same 0.0-delta timestamp as its neighbor (exactly how the first `Cred:` frame arrives) was misclassified as a duplicate and deleted. The drop test now compares CHARACTER CONTENT (`contents[idx]`), so a content-changing frame at a shared timestamp always survives while true identical repaints inside collapsed static runs are still removed. Verified: both new recordings keep the status line (mimo: statusline visible from 4.0s; DeepSeek: `Cred:` visible from 4.1s). The DeepSeek demo now lives in its own `docs/deepseek-demo.gif`; `docs/demo.gif` stays the main "What it looks like" demo. 192 tests pass |
| 64 | The main demo still had too much idle time; re-record it with the MIMO provider; demo recordings must not use `--provider` (prepare the selection beforehand) | ✅ DONE | Re-recorded BOTH demos inside fresh herdr panes (asciinema, 100x30) with plain `bobonyo` and the provider PRE-SELECTED in `~/.config/nanocoder/nanocoder-preferences.json` (no `--provider` flag appears in any recording). Main demo (`docs/demo.gif`, **13.28s**, 87KB): Xiaomi mimo-v2.5-pro → `bobonyo` → streamed `⚙ Thinking` + a haiku reply (`Keys clack in the void / Green text blooms on black canvas / Segfault. Try again.`) → `✦ Worked for a focused 3s. · 13393 tokens` → exit. Working phase is only ~3s so there is no idle-feeling wait. DeepSeek demo (`docs/deepseek-demo.gif`, **10.45s**, 63KB): DeepSeek pre-selected → `bobonyo` → status line shows `Cred: $5.06` → `Reply with just OK` → `✦ Worked for a focused 1s. · 13413 tokens · cache hit 99%` (warm prompt cache) → exit. README: "What it looks like" keeps `docs/demo.gif`; the **"DeepSeek, first class"** section embeds its own `docs/deepseek-demo.gif` with a caption explaining the live balance, `/models` catalog and warm-cache hit. `dist/bobonyo` rebuilt so the release alias includes the DeepSeek feature. Frame checks: no blank frames, no brightness anomalies, status line verified present in both GIFs. 192 tests pass |
| 65 | Update the "Configure Providers" README section: DeepSeek models are now fetched automatically, so the static `models` list should be optional | ✅ DONE | The config normalizer forced `['mock-model-1']` when a provider had no `models` list, and the initial model was chosen BEFORE the async catalog fetch landed, so a DeepSeek entry without `models` briefly started on mock-model-1. Fix: `src/deepseek.ts` exports `cachedDeepSeekModels` (fresh catalog from the DISK cache), and the provider init in `src/app.tsx` seeds the DeepSeek catalog from it before picking the preferred model; only when there is neither a static list nor a warm cache does it await `refreshDeepSeekModels` first (the later refresh call is deduped in-flight). The README "Configure Providers" section now shows the DeepSeek entry WITHOUT `models` and documents it as optional (static list = offline fallback). 2 new unit tests (`cachedDeepSeekModels` fresh + stale/absent). Verified live with a config that has NO DeepSeek models: the app picks `deepseek-v4-flash` from the warm cache (0 `mock-model-1` occurrences, `Cred:` still shown via the stale-balance fallback). 194 tests pass |
| 66 | Research: can Xiaomi MiMo fetch balance / token-plan usage with the `tp-…` API key (MiMo-Code repo)? If yes, mirror the DeepSeek `Cred: $n` feature | ✅ DONE (research) + PARTIAL (implementation) | Research verdict: NO key-based balance/quota endpoint exists. MiMo-Code (latest upstream Aug 10, checked fresh — the user's local copy was June 18) auths via OAuth only; its only token-plan surface is a promotional subscribe dialog. Every public implementation that shows MiMo quota (CodexBar, opencode-quota, codex-app-transfer, token-monitor, cc-toolkit, tmux-powerkit, conductor, Mimo-Token-Usage) hits `platform.xiaomimimo.com/api/v1/{balance,tokenPlan/detail,tokenPlan/usage}` with BROWSER COOKIES (`api-platform_serviceToken` + `userId`); codex-app-transfer even documents "tp- key cannot query quota, tested with key still 401". OmniRoute's `xiaomi-mimo.ts` states the same and self-tracks usage locally. Live probes with the user's real `tp-…` key: `token-plan-sgp…/v1/models` → 200 (full catalog); `/v1/balance`, `/user/balance`, `/v1/quota`, `/v1/plan`, `/v1/subscription`, `/v1/me` → 404; `platform.xiaomimimo.com/api/v1/*` (key auth) → 401 → Xiaomi account login. IMPLEMENTED instead: (a) MiMo model AUTO-DISCOVERY — config normalizer auto-sets `modelDiscoveryUrl` to `<base>/v1/models` for `token-plan-*.xiaomimimo.com` hosts, `discoverModels` now fetches the discovery URL AS-IS (was appending `/v1/models` to a URL that already ended in `/models` — latent bug that silently fell back to the static list for every explicit discovery URL), and the init block seeds/awaits the catalog from the shared disk cache exactly like DeepSeek (`refreshProviderModels`, reuses `deepseek-cache.json`); (b) LOCAL MONTHLY USAGE LEDGER — new `src/provider-usage.ts` accumulates every turn's `usage` block (prompt/completion/cache) into `~/.config/nanocoder/provider-usage.json` keyed by provider + UTC month, atomic temp+rename writes, survives restarts, multi-instance-safe (last-writer-wins, same policy as the DeepSeek cache); status line shows `· used 1.24M` (label secondary, amount primary, same two-tone treatment as `Cred:`), `/status` gains a "Provider usage" row, `/usage` prepends "This month: …". 16 new unit tests (isXiaomiMiMo detection, `refreshProviderModels` fetch/cache/fallback, discovery-URL contract, monthKey/formatTokens/accumulation/month rollover/restart survival, config auto-discovery URL, status rows). README consolidated the separate DeepSeek section into "DeepSeek and Xiaomi MiMo, first class" per the user's deduplication request. 211 tests pass |
| 67 | Compaction quality + harness request cache parity with codex (review the compact algo; ensure requests never miss the LLM cache) | ✅ DONE | Studied codex-rs `compact.rs` / `compact_token_budget.rs` / `context_window.rs` / `turn.rs`, the original nanocoder `tool-filter.ts` + `prompt-caching.ts` + `auto-compact.ts`, and our `client.ts`/`app.tsx`. GAPS FOUND + FIXED: (1) TOOL HEAD STABILITY — the tool definitions are the cache head (codex `prompt_tools_are_consistent_across_requests`, nanocoder tool-filter "any per-turn change busts the ENTIRE cache"); our `openAIToolBlocks` preserved registration order AND MCP/custom tools register lazily after startup, so a first request before loading finished sent a partial catalog that changed on the next turn. Fix: `openAIToolBlocks` now SORTS names (byte-identical head regardless of registration order, incl. MCP connect order) and `submit` gates the first LLM request on `startupReadyRef` (set when lazy MCP/skills/LSP loading settles) — `/` commands still work during loading. (2) COMPACTION STRUCTURE — codex caps verbatim user messages at 20K tokens (`COMPACT_USER_MESSAGE_MAX_TOKENS`, newest kept, oldest TRUNCATED to fit) and puts the summary LAST (directly above the next user message); we kept a fixed 6 user messages with no token cap and the summary FIRST. Fix: new `collectCompactedUserMessages` (token-budget selection + truncation, exported + unit-tested) and summary-last ordering. (3) SUMMARIZATION REQUEST — codex sends the FULL context and trims the OLDEST item on `ContextWindowExceeded` (preserving the cache head + recent messages); we pre-capped at 20K. Fix: `summarizeContext` now streams the full context and retries by trimming the oldest item on 400/413 (`isCompactOverflowError`, unit-tested). (4) DUPLICATE SYSTEM HEAD — compaction stored a system message inside context while the client prepends the system prompt on every request → the next turn (and every saved session after compaction) had a two-system head. Fix: the compacted history never carries a system message. Deliberately NOT changed: the `maxMessages` hard cap (silently drops the oldest → cache break) — parity with nanocoder's configurable safety valve, and token-based auto-compact fires first in normal use. 9 new unit tests (sorted tool head byte-stability, compacted-user-message budget/truncation/newest-first, no system in compacted history, overflow detection). 220 tests pass, build green |
| 68 | Space character does not work in modal search boxes (resume / model / settings) | ✅ DONE | Root cause: OpenTUI reports the space key as `name: 'space'` (the main input box handles it explicitly), but the modal search handlers only appended single-character `event.name` values — a space typed into ⌕ search did nothing. Fix: resume-modal, model-modal, settings-panel (and the new agents modal) all handle `event.name === 'space'` → append a literal `' '` to the query (same guard: !ctrl && !meta). Unit-covered via the shared search/scroll helpers; verified pattern matches the main input's `typedChar` handling. |
| 69 | Hilinga agents are missing: bobonyo does not load the nanocoder subagents (`.nanocoder/agents/*.md`) | ✅ DONE | The rewrite only knew the two built-in personalities (General/Explore); the original loads markdown subagents from the USER config dir (`~/.config/nanocoder/agents/`) and the PROJECT dir (`.nanocoder/agents/`, project wins). New `src/subagents.ts` ports the loader: `--- frontmatter ---` (name/description/model/tools/disallowedTools) + body = system prompt, priority project > user. Wired into: (a) the AGENTS modal — now lists ALL discovered agents (built-ins + custom, searchable, ↑/↓ + Enter opens the full system prompt in the details modal, source badge); (b) the `agent` tool — `runSubagent` resolves the discovered file's system prompt for the delegated agent instead of always using the built-in instruction. Verified live in `/mnt/data/KSProjects/Hilinga`: all 11 project agents discovered (Explore, general-purpose, hilinga-marketing-integrator, review-*), marketing prompt 10k chars loaded. 5 new unit tests (project discovery, user vs project override, frontmatter tools/model/inherit, prompt resolution + unknown fallback, name-less files skipped). |
| 70 | /settings rows fall back to a "set value" text prompt for VIEW surfaces (e.g. 38 custom commands) instead of the original UI flows | ✅ DONE | The original nanocoder opens managed PANELS for view/edit surfaces; the rewrite routed every non-option row into the pending-prompt. New generic `SettingsListModal` (centered card, ↑/↓ + mouse, scroll, Esc close, optional Enter action) and a settings-row → UI mapping in `editSettingRow`: Custom commands (list of /name + description), Skills, Custom tools, Sessions (list + Enter resumes), Checkpoints (list + Enter restores), Steering/InnerDaemon rules, MCP servers (with connected state), Background tasks, Providers (per-provider detail). Rows that are genuinely free-text values (Paste threshold, auto-compact threshold, watchdog, stream guard, privacy patterns, trusted dirs) keep the value editor — the prompt now shows the row label; Model/Vision/Web-search keep the model modal; option rows (Theme, Title shape, Status line, Mode, Tool profile) keep the in-modal selector. 1 new unit test (list scroll math). 229 tests pass, build green |
| 71 | Nitpick: display-only commands (/commands, /tools, /skills, …) still print into the chat history; route them to modals like /status | ✅ DONE | `/commands` now opens the SAME commands catalog modal the settings row uses; `/tools`, `/skills`, `/tasks`, `/sessions`, `/checkpoints`, `/mcp` open list modals; `/help` opens the command catalog; `/version`, `/credits`, `/doctor`, `/privacy`, `/statusline`, `/lsp`, `/innerdaemon`, `/schedule`, `/update`, `/context-max`, `/setup-config`, `/setup-mcp` open the scrollable details modal. Only conversation-relevant events (interrupts, renames, checkpoint saves, exports) still render in the transcript. |
| 72 | The `[command]`/`[skill]` tag in the `/` suggestions must be a PREFIX (before the description), not a suffix | ✅ DONE | The suggestion row rendered the tag right-aligned AFTER the description. Restructured: each suggestion is now a 2-line row — line 1 `❯ /name [Command] first-line-of-description`, line 2 the wrapped description continuation (up to 2 lines, `…` when truncated). The tag is only carried by non-built-in entries, as before. Popup height now sums the actual wrapped line counts (`completionPopupHeight(input, width)`) so the history layout stays accurate. |
| 73 | Custom commands list truncates command names (create-pr → create-) and selecting should insert into the input | ✅ DONE | Root cause: long descriptions rendered on ONE clipped line, so the row (and the perceived name) was cut mid-word. Fix: `wrapDescription` (new shared `src/description-wrap.ts`) wraps descriptions to at most 2 lines with `…`; settings list rows now render label + wrapped description lines, and the card height accounts for the 2-line rows. Selecting a custom command (Enter or click) inserts `/<name> ` into the user input and closes the modal (both keyboard and mouse paths; `/commands` modal too). Verified with Hilinga's real create-pr: full name preserved, description wraps cleanly. `wrapText`/`wrapTextDetailed` moved to `src/text-wrap.ts` to break the import cycle (history + input specs updated). 3 new unit tests (2-line wrap, ellipsis, empty), 232 tests pass |
| 74 | Settings/list modals: not centered (floats at the top), no search, not height-responsive, selection should close ALL modals; providers must be easy to edit (opencode-style) | ✅ DONE | (1) CENTERED: both the settings modal and the settings list modal now compute `cardY = (screenHeight − cardHeight)/2` (the settings modal was anchored at `height/4`). (2) SEARCH: the list modal gained a ⌕ search box (typing filters label+description live, Esc clears it before closing, space supported), same pattern as the settings tabs. (3) RESPONSIVE: `listVisible` scales with the terminal height (3…30 rows, capped so the card never overflows) instead of a fixed 6-20; the card grows with the window. (4) CLOSE-ALL: inserting a command, resuming a session, restoring a checkpoint, or editing a provider now closes BOTH the list modal and the settings modal (`setSettingsOpen(false)`), so nothing reappears underneath. (5) PROVIDER EDIT: selecting a provider row (Enter or click) closes the modals and opens the guided `/setup-providers edit <id>` wizard — Base URL (prefilled), API key (or env:VAR), Models — instead of a read-only detail. 2 new unit tests (search filter), 234 tests pass, build green |
| 75 | pane m2 review: (1) multi-line user messages miss background on breaklines, (2) user messages should cap at N lines with a clickable +N more lines modal, (3) harness misreads commands vs the original nanocoder, (4) triggered commands should render as tool-style `✦ Triggered a Command(name)` with only `Command` primary | ✅ DONE | Verified live in herdr pane m2 (w1:p1C): `/worktree` ran as a custom command and its body was echoed as a giant user message. (1) BREAKLINE BG: `tokenizeUserMessage` now applies the #2a2a2a surface to INTERIOR blank lines (index > 0) so multi-line user messages read as ONE solid block; only the leading separator row stays bg-free. (2) USER LINE CAP: `renderUserBlock` caps user messages at 12 display lines with a `… +N more lines` footer (secondary-dim inside the surface); the block is registered in `compactDetails` + `blockRanges`, so clicking it opens the FULL text in the details modal (title "User message"). (3) COMMAND DETECTION: custom commands and subscribe auto-triggers now carry `command: {kind, name, body}` metadata on the transcript message (parity: nanocoder's `[Executing custom command: …]` marker + UserMessage collapse), so the injected body is never echoed as a plain user message. (4) TOOL-STYLE ROW: new `commandrow` renderer — `✦ Triggered a Command(name)` / `Skill(name)` header with the FIRST 10 body lines under `  └   ` and a `+N more lines` footer; ONLY the word `Command`/`Skill` is primary bold, the glyph is status-green, everything else secondary (verified in the live ANSI capture: `Triggered a` + `(worktree)` secondary, `Command` primary-bold). Modal title "Triggered command". 5 new unit tests (command block cap/format, skill variant, user cap/format). 239 tests pass, build green |
| 76 | pane m2 follow-up: (1) `Triggered a` + `(name)` must be WHITE not secondary, (2) hover missing on command blocks, (3) /commands modal: 2-column command/description layout with categories and a taller responsive card, (4) arrow keys in modals still scroll the chat history | ✅ DONE | (1) COLORS: `tokenizeCommandRow` now renders `Triggered a ` and `(worktree)` in the default WHITE text color, `Command`/`Skill` primary bold (unit-tested, both with-glyph and glyph-less forms — liveRowSegments strips the glyph, which the old regex REQUIRED and silently fell back to all-primary headers; fixed the optional-glyph regex). Verified live ANSI: `Triggered a ` white, `Command` primary-bold, `(worktree)` white. (2) HOVER: command rows joined `COMPONENT_ROW_LANGS` + `liveRowSegments` (`commandrow` case), so they render through SettledToolRow — hover now applies the body-row background tint (header untouched), verified live with SGR mouse motion (body rows got `bg` 21,23,33). (3) /commands MODAL: new `CommandsModal` (grouped Built-in / Custom / Skills headers like the model selector), TWO-COLUMN rows — `/name` in a fixed 30-col column, description beside it wrapping up to 2 lines; search filters across categories; ↑/↓ + Enter inserts; responsive height uses nearly the full terminal (listVisible capped at 60, was 40; SettingsListModal raised 30 → 60 too). Verified live: 81 commands, both categories, wrapped descriptions, card uses the full height. (4) FOOLPROOF KEY ISOLATION: OpenTUI dispatches every key to ALL listeners — the history scrollbox natively scrolls on arrows (ScrollBar.handleKeyPress), and the old gates only prevented OUR handlers, not the scrollbox. Fix: the App/History/InputBox modal gates now call `event.preventDefault()` — global `useKeyboard` listeners run BEFORE the scrollbox's renderable handler, which skips `defaultPrevented` keys — so arrows used to navigate any modal can NEVER scroll the chat behind it. Verified live: arrow keys move the modal selection while the chat rows stay frozen. 2 new unit tests (header colors incl. glyph-less form), 241 tests pass, build green |
| 77 | pane m2 follow-up 2: (1) commands modal column-2 wrap escapes the column, (2) tool/bash output wrap escapes the `  └   ` indent container, (3) task details modal auto-closes | ✅ DONE | (1) COLUMN-2 WRAP: the commands modal's wrapped description continuation was indented only 3 spaces (under column 1); it now pads to the description column (32 cols = ❯ + command column) so wrapped text stays inside column 2. Verified live: `/create-pr` description wraps with the continuation aligned under the description, never the command column. (2) INDENT CONTAINER: `formatOutputTail` emitted raw output lines — a line longer than the container spilled past the `  └   ` edge. It now pre-wraps each output line (84 cols) with every continuation keeping the `      ` indent, and `wordWrap` HARD-SPLITS single words longer than the width (URLs/paths/log lines) instead of emitting one overflowing line — the same bug affected long bash commands. New unit test asserts every rendered tail line keeps the `└`/continuation prefix. (3) AUTO-CLOSE: root cause — the details modal opens on the row's mouse-DOWN, and the SAME click's mouse-UP lands on the modal backdrop, which closes it instantly when the click point is outside the card (top-of-screen rows). All 9 modals with a backdrop `onMouseUp` close handler now ignore the FIRST mouse-up after mount (one-shot `suppressFirstMouseUp`), so the opening click can never close the modal it just opened; subsequent backdrop clicks still close. 1 new unit test (wrap containment), 242 tests pass, build green |
| 78 | Active task list (○ scan · ○ write …) renders inside the user input box; it should not be there | ✅ DONE | Removed the live task-list overlay from the input box (the running-task spinner header + the ○/✓ task list row) — the input box now only ever shows the prompt border, typed text, and the model/ctx corner. The task list still lives where it belongs: the `✦ Tasks` block in the chat history (from write_tasks) and `/tasks` modal. `computeInputBoxHeight` no longer reserves rows for the overlay (signature simplified, callers updated). Verified live in preview: after /mock:tasks the input box shows just the prompt border, no ○/✓ rows. 242 tests pass, build green |
| 79 | Details modal is not responsive — it should use the available height like the commands modal | ✅ DONE | The details modal card had a hard 30-row cap (`Math.min(30, available)`); it now uses nearly the whole terminal height (`dims().height - 2`, same policy as the commands modal), so long tool/thought/command details need less scrolling. Width stays capped at 96. 242 tests pass, build green |
| 80 | Running `/worktree purpose: hello world` shows ONLY the Triggered a Command block — the original typed message should appear in the history before it | ✅ DONE | The transcript message for a custom command carried the command metadata but its content was the substituted BODY, so the history rendered just the command block (the original typed command was never stored). Fix: the command metadata now carries `original` (the typed `/worktree purpose: hello world`); `submit` passes it as the transcript value while the provider still sees the substituted body, and the history renders TWO rows — the original typed command as a normal `❯` user message, then the `✦ Triggered a Command(worktree)` block with the body preview. Subscribe auto-triggers also carry the triggering text. Verified live: `/worktree purpose: hello world` renders `❯ /worktree purpose: hello world` first, then the command block. 242 tests pass, build green |
| 81 | `/worktree` does not accept arguments; refine the algorithm or the command file so the argument (task / purpose / worktree name) is understood, remove the `/create-pr` line, and default the base to the remote staging branch | ✅ DONE | (1) ALGORITHM: custom-command argument mapping only supported ONE-TOKEN positional args, and a file with no `arguments:` frontmatter silently DROPPED the user's args (the body never saw them). Added `rest: true` argument support — a rest arg captures ALL remaining tokens as one value — extracted into pure `mapCommandArguments` (3 new unit tests: rest capture, positional+rest mix, empty tokens). (2) COMMAND FILE (`Hilinga/.nanocoder/commands/worktree.md`): declares `arguments: [{name: request, rest: true}]` and the body now includes `The user's request is: {{request}}` plus instructions to interpret it as a TASK/PURPOSE (derive a kebab-case name) or an explicit NAME (use verbatim); removed `Use /create-pr when the work is ready.`; Base selection now explicitly defaults to the REMOTE `staging` branch unless the user provides a different base (e.g. "base from the latest remote main branch"). Verified live: `/worktree purpose: hello world` renders the command block with `The user's request is: purpose: hello world` in the preview, and the file no longer references /create-pr. 245 tests pass, build green |
| 82 | (1) Background task completion row is plain/unformatted, (2) clickable rows open the details modal instantly so dragging/selecting is impossible | ✅ DONE | (1) FORMAT: the background completion row now renders in the TOOL-ROW visual language (parity: nanocoder's BackgroundTaskCompleted) — `✦ Background task completed · exit N` header via the toolrow tokenizer (green glyph + white text), the script under a `  └   ` container with hard-wrap (long commands/URLs can never escape the indent), collapsed to 3 lines with a `… +N more lines` footer; the existing ctrl+o/click expand still works. 2 new unit tests (basic format + hard-wrap containment). (2) DRAG GUARD: the details modal previously opened on mouse-DOWN, so any press (even a drag to select text) popped it. Now mouse-down only RECORDS the press + pending range; the modal/expand fires on MOUSE-UP only when the pointer did NOT move more than 3px (a drag cancels the pending click via the mouse-move handler). Verified live: a click opens the Bash ×10 details modal; a press-move-release drag leaves the chat untouched and allows selection. 247 tests pass, build green |

## Implementation notes (committed)

- `src/state.ts`: pending queue entries carry attachments (queued `[Image #N]`
  prompts keep their image paths for vision analysis).
- `src/vision.ts`: vision fallback analysis (OpenAI-compatible chat completions
  with `image_url` content parts) through the configured vision model.
- `src/web-search.ts`: native web-search fallback (Responses API) through the
  configured search model, with the chat indicator.
- `src/config.ts`: `savePreferences` merges (fallback keys survive model
  switches); provider `baseUrl` normalization strips a trailing `/v1` (the
  client appends `/v1/chat/completions` — the doubled path 404s on Xiaomi's
  token-plan endpoint and silently fell back to another provider).
- `src/client.ts`: system prompts updated to BoboNyo; OpenAI request body
  carries standard `tools` + `tool_calls` shapes.
- `src/components/history.tsx`: transcript renders as multiple markdown blocks;
  replies get padded containers with the glyph outside; SETTLED vs LIVE blocks
  are split so streaming never re-renders settled content (flash fix); mouse
  hover/click maps through per-block row offsets; all expandable content
  (thoughts, tool rows, compact tallies) opens the DetailsModal; collapsed
  previews cap at 3 lines; user-message tokens colored from real attachments.
- `src/components/details-modal.tsx`: scrollable, colored (primary tool names,
  secondary output), keys isolated from the chat behind it.
- `src/components/input-box.tsx`: trailing-space wrap fix; attachments passed
  through submit; box-background caret (typing pauses the blink via debounce);
  fast-erase backspace; suggestions borderless + scrollable.
- `src/components/input-box.tsx`: trailing-space wrap fix; attachments passed
  through submit; suggestions borderless + scrollable.
- `src/components/details-modal.tsx`: compact-tally click opens a scrollable
  details modal.
- `src/commands.ts` + `src/app.tsx`: `/codex` provider scaffold; `connectCodex`
  flow; status label; diagnostics only surface findings.

Gate: `bun test` (100 pass) + `bun run typecheck` clean before every commit.
